import React from 'react';
import Icon from 'react-native-vector-icons/FontAwesome';

const PauseButtonTemplate = ({size, color}) => (
  <Icon name="pause" size={size} color={color} />
);

export default PauseButtonTemplate;
