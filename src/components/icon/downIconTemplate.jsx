import React, { useState } from 'react';
import Icon from 'react-native-vector-icons/FontAwesome';


const DownIconTemplate = ({size, color}) => (
  <Icon name="chevron-down" size={size} color={color}/>
);

export default DownIconTemplate;