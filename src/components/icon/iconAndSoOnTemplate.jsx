import React, { useState } from 'react';
import Icon from 'react-native-vector-icons/FontAwesome';


const IconAndSoOnTemplate  = ({color, size}) => (
  <Icon name="ellipsis-h" size={size} color={color} />
);

export default IconAndSoOnTemplate ;