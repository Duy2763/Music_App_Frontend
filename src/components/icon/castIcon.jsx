import React, { useState } from 'react';
import Icon from 'react-native-vector-icons/MaterialIcons';

const CastIcon = () => (
  <Icon name="cast" size={25} color="gray" />
);

export default CastIcon;             