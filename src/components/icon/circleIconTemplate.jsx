import React, { useState } from 'react';
import Icon from 'react-native-vector-icons/MaterialIcons';


const CircleIconTemplate  = ({size, color}) => (
  <Icon name="circle" size={size} color={color} />
);

export default CircleIconTemplate ;