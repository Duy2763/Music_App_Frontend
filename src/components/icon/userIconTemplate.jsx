import React, { useState } from 'react';
import Icon from 'react-native-vector-icons/MaterialIcons';

const UserIconTemplate = ({size, color}) => (
  <Icon name="group" size={size} color={color} />
);

export default UserIconTemplate;             