import React, { useState } from 'react';
import Icon from 'react-native-vector-icons/FontAwesome';


const SearchIconTemplate  = ({size, color}) => (
  <Icon name="search" size={size} color={color} />
);

export default SearchIconTemplate ;