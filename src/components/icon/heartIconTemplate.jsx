import React, { useState } from 'react';
import Icon from 'react-native-vector-icons/FontAwesome';


const HeartIconTemplate = ({size, color}) => (
  <Icon name="heart" size={size} color={color} />
);

export default HeartIconTemplate;