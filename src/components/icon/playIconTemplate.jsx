import React from 'react';
import Icon from 'react-native-vector-icons/FontAwesome';

const PlayButtonTemplate = ({size, color}) => (
  <Icon name="play" size={size} color={color} />
);

export default PlayButtonTemplate;
